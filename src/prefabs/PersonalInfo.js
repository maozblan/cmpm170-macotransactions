class PersonalInfo {
    constructor(scene, x, y) {
        //handle default args
        this.scene = scene
        this.x = x
        this.y = y

        //add it to the scene
        //scene.add.existing(this)

        //properties
        this.cashOnHand = this.cashOnHand

    }

    create() {

    }
}